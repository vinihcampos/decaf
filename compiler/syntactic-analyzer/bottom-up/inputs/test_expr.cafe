int a;

double a(){
	if(a < 32)
		if(a = 1 && 43 && a && this || a() || 67+34-132*34/75%1 || -(a < 3) || 67 <= 32 || b > c  && h <= 67 || a == 23 && a != 23 || !a && readInteger() || readLine() && new(a) || newArray(45, bool)  )
			a = 42;
		else
			a = "else";
}

